@model IEnumerable<SmilyAccountant.Areas.Finance.Models.FixedAssetCard>

@{
    ViewData["Title"] = "Index";
}

<h1>Fixed Asset Cards</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FAClassCodeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FASubClassCodeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SerialNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FAClassCodeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FASubClassCodeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SerialNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FixedAssetCardId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FixedAssetCardId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FixedAssetCardId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
